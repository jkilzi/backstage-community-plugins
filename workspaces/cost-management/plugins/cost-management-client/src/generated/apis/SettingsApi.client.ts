//

// ******************************************************************
// * THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY. *
// ******************************************************************
import { DiscoveryApi } from '../types/discovery';
import { FetchApi } from '../types/fetch';
import crossFetch from 'cross-fetch';
import {pluginId} from '../pluginId';
import * as parser from 'uri-template';

import { CostGroupsResponse } from '../models/CostGroupsResponse.model';
import { GetAwsCategorySettings } from '../models/GetAwsCategorySettings.model';
import { ModifyKeySettingsRequestBody } from '../models/ModifyKeySettingsRequestBody.model';
import { PutSettingsCostGroupsRequestInner } from '../models/PutSettingsCostGroupsRequestInner.model';
import { PutSettingsTagsEnableRequest } from '../models/PutSettingsTagsEnableRequest.model';
import { PutSettingsTagsMappingsParentRequest } from '../models/PutSettingsTagsMappingsParentRequest.model';
import { SettingIn } from '../models/SettingIn.model';
import { SettingOut } from '../models/SettingOut.model';
import { SettingsTagsMappingsOptionsResponse } from '../models/SettingsTagsMappingsOptionsResponse.model';
import { SettingsTagsMappingsResponse } from '../models/SettingsTagsMappingsResponse.model';
import { SettingsTagsResponse } from '../models/SettingsTagsResponse.model';

/**
 * Wraps the Response type to convey a type on the json call.
 *
 * @public
 */
export type TypedResponse<T> = Omit<Response, 'json'> & {
  json: () => Promise<T>;
};


/**
 * Options you can pass into a request for additional information.
 *
 * @public
 */
export interface RequestOptions {
  token?: string;
}

/**
 * no description
 */
export class SettingsApiClient {
    private readonly discoveryApi: DiscoveryApi;
    private readonly fetchApi: FetchApi;

    constructor(options: {
        discoveryApi: { getBaseUrl(pluginId: string): Promise<string> };
        fetchApi?: { fetch: typeof fetch };
    }) {
        this.discoveryApi = options.discoveryApi;
        this.fetchApi = options.fetchApi || { fetch: crossFetch };
    }

    /**
     * Assign to cost management settings
     * @param settingIn Application settings that needs to stored
     */
    public async assignSettings(
        // @ts-ignore
        request: {
                body: SettingIn,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'POST',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Query to cost management settings
     */
    public async getAwsCategoryKeysSettings(
        // @ts-ignore
        request: {
        },
        options?: RequestOptions
    ): Promise<TypedResponse<Array<GetAwsCategorySettings> >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/aws_category_keys/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'GET',
            
        });
    }

    /**
     * Query to cost management settings
     */
    public async getSettings(
        // @ts-ignore
        request: {
        },
        options?: RequestOptions
    ): Promise<TypedResponse<Array<SettingOut> >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'GET',
            
        });
    }

    /**
     * Query which projects belong to which cost groups
     * @param offset Parameter for selecting the offset of data.
     * @param limit Parameter for selecting the amount of data in a returned.
     * @param filter The filter to apply to the report as a URL encoded dictionary.
     */
    public async getSettingsCostGroups(
        // @ts-ignore
        request: {
            query: {
                offset?: number,
                limit?: number,
                filter?: any,
            },
        },
        options?: RequestOptions
    ): Promise<TypedResponse<CostGroupsResponse >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/cost-groups/{?offset,limit,filter}`;

        const uri = parser.parse(uriTemplate).expand({
            ...request.query,
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'GET',
            
        });
    }

    /**
     * Query tags in Cost Management from cloud sources
     * @param offset Parameter for selecting the offset of data.
     * @param limit Parameter for selecting the amount of data in a returned.
     * @param sourceType Cloud provider type
     * @param enabled Enable state of the tag
     * @param orderBy Field(s) used to sort results. Add a &#39;-&#39; prefix for descending order.
     * @param uuid UUID of a tag entry
     * @param key Case insensitive search of key values. One or more key parameters may be provided.
     */
    public async getSettingsTags(
        // @ts-ignore
        request: {
            query: {
                offset?: number,
                limit?: number,
                sourceType?: string,
                enabled?: boolean,
                orderBy?: Array<string>,
                uuid?: Array<string>,
                key?: Array<string>,
            },
        },
        options?: RequestOptions
    ): Promise<TypedResponse<SettingsTagsResponse >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/{?offset,limit,source_type,enabled,order_by*,uuid*,key*}`;

        const uri = parser.parse(uriTemplate).expand({
            ...request.query,
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'GET',
            
        });
    }

    /**
     * Returns relationship mappings for Tag Reconciliation
     * @param offset Parameter for selecting the offset of data.
     * @param limit Parameter for selecting the amount of data in a returned.
     * @param sourceType Cloud provider type
     * @param parent parent key value
     * @param child child key value
     * @param orderBy Field(s) used to sort results. Add a &#39;-&#39; prefix for descending order.
     */
    public async getSettingsTagsMappings(
        // @ts-ignore
        request: {
            query: {
                offset?: number,
                limit?: number,
                sourceType?: string,
                parent?: string,
                child?: string,
                orderBy?: Array<string>,
            },
        },
        options?: RequestOptions
    ): Promise<TypedResponse<SettingsTagsMappingsResponse >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/mappings/{?offset,limit,source_type,parent,child,order_by*}`;

        const uri = parser.parse(uriTemplate).expand({
            ...request.query,
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'GET',
            
        });
    }

    /**
     * Returns available options for tags that can be marked as a child.
     * @param offset Parameter for selecting the offset of data.
     * @param limit Parameter for selecting the amount of data in a returned.
     * @param sourceType Cloud provider type
     * @param parent parent key value
     * @param child child key value
     * @param orderBy Field(s) used to sort results. Add a &#39;-&#39; prefix for descending order.
     */
    public async getSettingsTagsMappingsChild(
        // @ts-ignore
        request: {
            query: {
                offset?: number,
                limit?: number,
                sourceType?: string,
                parent?: string,
                child?: string,
                orderBy?: Array<string>,
            },
        },
        options?: RequestOptions
    ): Promise<TypedResponse<SettingsTagsMappingsOptionsResponse >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/mappings/child/{?offset,limit,source_type,parent,child,order_by*}`;

        const uri = parser.parse(uriTemplate).expand({
            ...request.query,
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'GET',
            
        });
    }

    /**
     * Returns available options for tags that can be marked as a parent.
     * @param offset Parameter for selecting the offset of data.
     * @param limit Parameter for selecting the amount of data in a returned.
     * @param sourceType Cloud provider type
     * @param parent parent key value
     * @param child child key value
     * @param orderBy Field(s) used to sort results. Add a &#39;-&#39; prefix for descending order.
     */
    public async getSettingsTagsMappingsParent(
        // @ts-ignore
        request: {
            query: {
                offset?: number,
                limit?: number,
                sourceType?: string,
                parent?: string,
                child?: string,
                orderBy?: Array<string>,
            },
        },
        options?: RequestOptions
    ): Promise<TypedResponse<SettingsTagsMappingsOptionsResponse >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/mappings/parent/{?offset,limit,source_type,parent,child,order_by*}`;

        const uri = parser.parse(uriTemplate).expand({
            ...request.query,
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'GET',
            
        });
    }

    /**
     * Disable aws category key by uuid.
     * @param modifyKeySettingsRequestBody Disable aws category key by uuid
     */
    public async putDisableAwsCategoryKeysSettings(
        // @ts-ignore
        request: {
                body: ModifyKeySettingsRequestBody,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/aws_category_keys/disable/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Enable aws category key by uuid.
     * @param modifyKeySettingsRequestBody Enable aws category key by uuid
     */
    public async putEnableAwsCategoryKeysSettings(
        // @ts-ignore
        request: {
                body: ModifyKeySettingsRequestBody,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/aws_category_keys/enable/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Add projects to a cost group
     * @param putSettingsCostGroupsRequestInner List of project name and cost group
     */
    public async putSettingsCostGroups(
        // @ts-ignore
        request: {
                body: Array<PutSettingsCostGroupsRequestInner>,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/cost/groups/add/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Remove projects from a cost group
     * @param putSettingsCostGroupsRequestInner List of project name and cost group
     */
    public async putSettingsCostGroupsRemove(
        // @ts-ignore
        request: {
                body: Array<PutSettingsCostGroupsRequestInner>,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/cost-groups/remove/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Disable tags in Cost Management
     * @param putSettingsTagsEnableRequest List of tag UUIDs
     */
    public async putSettingsTagsDisable(
        // @ts-ignore
        request: {
                body: PutSettingsTagsEnableRequest,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/disable/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Enable tags in Cost Management
     * @param putSettingsTagsEnableRequest List of tag UUIDs
     */
    public async putSettingsTagsEnable(
        // @ts-ignore
        request: {
                body: PutSettingsTagsEnableRequest,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/enable/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Removes child tags from mapping.
     * @param putSettingsTagsEnableRequest List of tag UUIDs
     */
    public async putSettingsTagsMappingsChildRemove(
        // @ts-ignore
        request: {
                body: PutSettingsTagsEnableRequest,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/mappings/child/remove/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Adds child tags to a parent tag.
     * @param putSettingsTagsMappingsParentRequest List of tag UUIDs
     */
    public async putSettingsTagsMappingsParent(
        // @ts-ignore
        request: {
                body: PutSettingsTagsMappingsParentRequest,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/mappings/child/add/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

    /**
     * Removes all parent and child tags.
     * @param putSettingsTagsEnableRequest List of tag UUIDs
     */
    public async putSettingsTagsMappingsParentRemove(
        // @ts-ignore
        request: {
                body: PutSettingsTagsEnableRequest,
        },
        options?: RequestOptions
    ): Promise<TypedResponse<void >> {
        const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

        const uriTemplate = `/settings/tags/mappings/parent/remove/`;

        const uri = parser.parse(uriTemplate).expand({
        })

        return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
            headers: {
                'Content-Type': 'application/json',
                ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
            },
            method: 'PUT',
             body: JSON.stringify(request.body), 
        });
    }

}
